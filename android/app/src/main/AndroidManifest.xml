<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fatima_mill">

    <!-- ✅ Internet permission (required for fetch, axios, etc) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ✅ Storage permissions for Android < 13 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- ✅ Android 13+ media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- ✅ Notification + Wake Lock -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- React Native Notifications (FCM services removed because Firebase is not configured)
             If you later configure Firebase properly, you can re-add these services. -->

        <!-- Explicitly remove FCM-related services/receivers from the library manifest -->
        <service
            android:name="com.wix.reactnativenotifications.fcm.FcmInstanceIdListenerService"
            tools:node="remove" />

        <service
            android:name="com.wix.reactnativenotifications.fcm.FcmMessagingService"
            tools:node="remove" />

        <service
            android:name="com.wix.reactnativenotifications.fcm.FcmInstanceIdRefreshHandlerService"
            tools:node="remove" />

        <receiver
            android:name="com.wix.reactnativenotifications.notification.NotificationReceiver"
            tools:node="remove" />

        <receiver
            android:name="com.wix.reactnativenotifications.notification.ActionReceiver"
            tools:node="remove" />

    </application>

</manifest>